package com.example.kasirku.ui.theme

import androidx.compose.ui.graphics.Color

// Palet Warna Utama Aplikasi
val PrimaryRed = Color(0xFFD02A2A) // Merah tua yang kuat untuk tombol dan elemen utama
val PrimaryRedLight = Color(0xFFFF6659) // Varian merah yang lebih terang
val TextBlack = Color(0xFF1D1D1D) // Hitam pekat untuk teks
val BackgroundLight = Color(0xFFF5F5F5) // Abu-abu sangat terang untuk background
val SurfaceWhite = Color(0xFFFFFFFF) // Putih bersih untuk card atau surface
val BorderGray = Color(0xFFE0E0E0) // Abu-abu untuk garis pemisah atau border
val TextGray = Color(0xFF757575) // Abu-abu untuk teks sekunder

// Warna untuk Dark Theme
val DarkBackground = Color(0xFF121212) // Latar belakang hitam untuk mode gelap
val DarkSurface = Color(0xFF1E1E1E) // Surface yang sedikit lebih terang dari background
val PrimaryRedDark = Color(0xFFD02A2A) // Merah yang lebih lembut untuk mode gelap

// Warna Status & Alert
val StatusSuccess = Color(0xFF2E7D32) // Hijau untuk notifikasi sukses
val StatusWarning = Color(0xFFEF6C00) // Oranye untuk notifikasi peringatan
val StatusError = Color(0xFFC62828)   // Merah untuk notifikasi error (mirip dengan primer)
val StatusInfo = Color(0xFF0277BD)     // Biru untuk notifikasi informasi